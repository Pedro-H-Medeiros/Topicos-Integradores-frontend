import { Open_Sans as OpenSans, Inter } from 'next/font/google'

export const openSans = OpenSans({
  subsets: ['latin'],
  display: 'swap',
})

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
